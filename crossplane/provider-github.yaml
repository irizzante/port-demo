apiVersion: github.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: github-provider
spec:
  credentials:
    source: Secret
    secretRef:
      name: github-provider
      namespace: crossplane-system
      key: credentials
