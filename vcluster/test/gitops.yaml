apiVersion: irizzant.it/v1alpha1
kind: GitOps
metadata:
    name: gitops
spec:
  id: vcluster-test
  kubeConfig:
    secretKey: config
    secretName: vc-test
  gitOpsRepo: https://github.com/irizzante/port-demo.git
  gitOpsPath: vcluster/test/argocd
  gitOpsSecret: gitlab-secret